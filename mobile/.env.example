# --- FoodSupply IQ mobile (Expo) ---
# Base URL the device/emulator uses to reach the FastAPI backend.
# Android emulator: use http://10.0.2.2:8000 ; physical device: use your LAN IP.
EXPO_PUBLIC_API_URL=http://localhost:8000

# Mapbox token for the rep map (Epic 5)
EXPO_PUBLIC_MAPBOX_TOKEN=
